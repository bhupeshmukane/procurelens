# ProcureLens Environment Configuration
# Note: ProcureLens features offline deterministic extraction and demo seeding.
# LLM API keys are OPTIONAL and only used if live AI document parsing is desired.

# Server Configuration
PORT=8000
HOST=127.0.0.1

# Optional AI / LLM Provider Keys
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
